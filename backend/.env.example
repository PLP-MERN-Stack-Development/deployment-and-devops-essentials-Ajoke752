# Backend environment variables example
# Copy to backend/.env and fill values before running

FRONTEND_URL=http://localhost:8080
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxx.mongodb.net/ecobarterDb?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_strong_secret_here
PORT=5000

# Notes:
# - Replace <username> and <password> in MONGO_URI with your Atlas credentials.
# - JWT_SECRET should be a long random string (32+ bytes). Do NOT commit secrets to git.
